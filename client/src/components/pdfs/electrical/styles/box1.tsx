import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
    imageContainer: {
        width: '100%',
        height: '50%',
        marginTop: 50,
    },
});

export default styles;
